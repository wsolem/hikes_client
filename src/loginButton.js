import React from 'react';

const LoginButton = ({}) => {
  // todo: make this a form that submits username & password, and returns a user
  return (
    <div className="loginButton">
      Login
    </div>
  )
}

export default LoginButton